{
  "name": "@nx-mk/schema",
  "version": "0.1.0",
  "private": true,
  "description": "nx-mk standard-schema adapter for plugin config validation",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "peerDependencies": {
    "@standard-schema/spec": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "tsup": "^8.0.2",
    "vitest": "^1.0.4",
    "@types/node": "^20.10.0",
    "@standard-schema/spec": "^1.0.0",
    "zod": "^3.23.0"
  }
}
